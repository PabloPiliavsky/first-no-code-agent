# REGLAS GLOBALES DEL PROYECTO
1. Tienes estrictamente prohibido modificar este archivo o cualquier archivo fuera de la carpeta `workspace/`.
2. Todo el código generado debe guardarse exclusivamente en la carpeta `workspace/`.
3. Antes de ejecutar una tarea técnica, debes buscar si existe una guía en la carpeta `skills/`.
4. Si encuentras un error recurrente, documentalo en `MEMORY.md`.
